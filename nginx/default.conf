server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;
    include mime.types;

    location / {
        try_files $uri $uri/ /index.html =404;
    }

    location /query {
        try_files $uri $uri/ /index.html =404;
    }
}
